Worst time complexity solution
Made too much use of inbuilt functions
\B Could be done by one pass through all or any

class Solution:
    def isMonotonic(self, A):
        """
        :type A: List[int]
        :rtype: bool
        """
        if A==sorted(A) or A == sorted(A)[::-1]:
            return True
        else:
            return False
                
