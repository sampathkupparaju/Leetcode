1. if the number is 1 return true
2. if the nummber is 0 return false
3. If the remainder after dividing the number by 2 is 0
      then recursively divide the number by 2 until it becomes 1 or
          until the remainder is not equal to zero in which we return false
   
   
   
   class Solution {
    public boolean isPowerOfTwo(int n) {
        if (n==1)
            return true;
        else if(n==0)
            return false;
        else if (n%2==0)
            return isPowerOfTwo(n/2);
       
    return false;
  
        
    }
}
